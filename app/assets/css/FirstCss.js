import { StyleSheet, Dimensions } from 'react-native';
import { wholeWidth, wholeHeight } from './MainCss';

export default StyleSheet.create({
    topPart: { position: 'absolute', top: -40, width: wholeWidth },
});
